<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.market">
	
	<insert id="insertNewProduct" parameterType="productDto">
		INSERT INTO
				T_PRODUCT (
					PRODUCT_NAME,
					PRODUCT_SELLER,
					PRODUCT_PRICE,
					PRODUCT_SORT,
					PRODUCT_DESC,
					PRODUCT_IMAGES,
					PRODUCT_CREDATE,
					PRODUCT_COMMENTS,
					PRODUCT_VIEWS,
					PRODUCT_LIKES
				)
		VALUES (
					#{productName},
					#{productSeller},
					#{productPrice},
					#{productSort},
					#{productDesc},
					#{productImages},
					NOW(),
					0,
					0,
					0
				);
	</insert>
	
	<select id="getAllProducts" resultType="ProductDto">
		SELECT
				*
		FROM
				T_PRODUCT;
	</select>
	
	<select id="getOneProduct" parameterType="int" resultType="ProductDto">
		SELECT
				*
		FROM
				T_PRODUCT
		WHERE
				PRODUCT_ID = #{num};
	</select>
	
	<select id="getSortedProducts" parameterType="String" resultType="ProductDto" >
		SELECT
				*
		FROM
				T_PRODUCT
		WHERE
				PRODUCT_SORT = #{category};
	</select>
	
	<update id="increaseViews" parameterType="int">
		UPDATE
				T_PRODUCT
		SET
				PRODUCT_VIEWS = PRODUCT_VIEWS + 1 
		WHERE
				PRODUCT_ID = #{num};
	</update>
	
</mapper>